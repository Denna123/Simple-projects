import React from "react";

function Footer() {
    // let d = getdate();
    // let currentYear = SELECT d.(getdate());

    return (
    // <p> copyright, {currentYear}</p>
    <p>Footer Here</p>
    );
}

export default Footer;
